set guioptions-=T
set guioptions-=m
set guioptions-=r
set guioptions-=L
set langmenu=en_US.UTF-8

if has("gui_running")
    if has("gui_win32")
        set guifont=Consolas:h11:cRUSSIAN
    else
        set guifont=Monospace
    endif
endif

